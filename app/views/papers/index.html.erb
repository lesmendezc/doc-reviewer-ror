<p id="notice"><%= notice %></p>

<h3 align="center">Documentos</h3>

<%= link_to new_paper_path, class: "btn-small waves-effect right" do %>
  <i class="material-icons left">add</i>
  Añadir
<% end %>
<br><br>

<table>
  <thead class="header-row">
    <tr>
      <th>No.</th>
      <th>Título</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @papers.each_with_index do |paper, index| %>
      <tr>
        <td><%= index+1 %></td>
        <td class="title-row"><%= link_to paper.title, paper, class: 'link-to-text' %></td>
        <td>
          <%= link_to rails_blob_path(paper.pdf_file, disposition: "inline"), 
           class: "waves-effect btn-floating btn-small grey", target: "_blank" do %>
            <i class="material-icons">picture_as_pdf</i>
          <%end%>
        </td>
        <td>
          <%= link_to paper_comments_path(paper), class: "waves-effect btn-floating btn-small grey" do %>
            <i class="material-icons">search</i>
          <% end %>
        </td>
        <td><a href="/reviews/1" class="waves-effect btn-floating btn-small grey"><i class="material-icons">add_box</i></a></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

